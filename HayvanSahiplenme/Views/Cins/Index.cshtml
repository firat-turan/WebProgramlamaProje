@model IEnumerable<HayvanSahiplenme.Models.Cins>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create" class="text-danger font-weight-bold" >Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CinsAd)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CinsAdIng)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Tur)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CinsAd)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CinsAdIng)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Tur.TurAd)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.CinsId" class="text-danger">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.CinsId" class="text-danger">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.CinsId" class="text-danger">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
